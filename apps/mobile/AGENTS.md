# apps/mobile

KMP (Kotlin Multiplatform) mobile app placeholder. Not yet scaffolded.

## Rules

- API types generated from `specs/api.openapi.yaml` via openapi-generator
- Design tokens will come from `packages/tokens/` (future)
- Nx targets defined in `project.json` — will invoke Gradle when scaffolded

## Setup

See `README.md` in this directory for scaffolding instructions.
